export const STUDENTS_QUERY_KEY = ['students'] as const;

export const STUDENTS_SHARED_SELECT =
  'id, name, aadhar_number, program, join_date, parent_name, parent_contact, parent_email, profile_image_url, created_at, default_monthly_fee, discount_percent';
